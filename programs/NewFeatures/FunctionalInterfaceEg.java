package NewFeatures;
/* main class :interface(FunctionalInterfaceEg)
 * abstract method  means it have only function not implementation
 * Functional Interface : will have only one abstract method 
 * sub class : WithoutLambda
 */
@FunctionalInterface
public interface FunctionalInterfaceEg {
	int calcAdd(int a, int b);
}
